import React from "react";

import classes from "./Footer.module.css";

const footer = () => (
  <footer className={classes.Footer}>
    <p className={classes.Title}>FOOTER</p>
  </footer>
);

export default footer;
