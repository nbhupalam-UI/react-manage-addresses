import React from "react";

import classes from "./Sidebar.module.scss";

const Sidebar = () => {
  return (
    <div className={classes.Sidebar}>
      <p className={classes.Title}>SIDEBAR</p>
    </div>
  );
};

export default Sidebar;
